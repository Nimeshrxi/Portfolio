# Character assets

Place realistic live-action Spider-Man PNG screenshots here (not cartoon / comic art).

Copy reference screenshots as:

| File | Suggested pose / use |
|------|----------------------|
| `spidey-idle.png` | Standing / default rooftop presence |
| `spidey-rooftop.png` | Crouched on rooftop (hero) |
| `spidey-wall.png` | Wall-crawl (about section) |
| `spidey-coding.png` | At workstation / coding scene |
| `spidey-hanging.png` | Upside-down hang (contact) |
| `spidey-landing.png` | Landing / impact pose (intro) |

Components reference paths like `/character/spidey-idle.png` with graceful fallbacks when a file is missing.

Parent agent / you should copy real PNGs into this folder; keep filenames exact so the character registry resolves them.
